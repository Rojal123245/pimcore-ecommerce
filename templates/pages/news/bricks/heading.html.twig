{% extends 'pages/news/bricks/base.html.twig' %}

{% block content %}
{% set text = brick.text %}
{% set heading_tag = brick.tag|default('h1') %}

<div class="w-full">
    <div class="container  mx-auto px-sm lg:px-0 lg:max-w-[60%]">
        <{{ heading_tag }} class="text-{{ heading_tag }}"> {{ text }} </{{ heading_tag }}>
    </div>
</div>

{% endblock %}
